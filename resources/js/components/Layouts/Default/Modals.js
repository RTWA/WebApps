import React from 'react';
import { PreviewModal } from '../../Routes/Blocks';

const Modals = () => {
    
    return (
        <>
            <PreviewModal />
        </>
    );
};

export default Modals;